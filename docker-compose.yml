services:
  ollama:
    image: ollama/ollama
    ports: ["11434:11434"]   # optional: expose for host testing
  app:
    build: .
    image: multi-pdf-chat:latest
    ports: ["8501:8501"]
    environment:
      OLLAMA_BASE_URL: http://ollama:11434
    depends_on: [ollama]